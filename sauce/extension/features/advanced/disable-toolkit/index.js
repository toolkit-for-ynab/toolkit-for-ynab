import { Feature } from 'toolkit/core/extension/feature';

export class DisableToolkit extends Feature {
  invoke() {
    // this is a empty class to make use of the Feature setting for localStorage.
  }
}
