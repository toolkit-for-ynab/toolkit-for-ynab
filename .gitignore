# OSX is a troll
.DS_Store
.project

# don't want to push node_modules :O
node_modules/

# extension deploy stuff
certificates/
save-commands.json

# python files
.python-version
*.pyc

# we use yarn -- ignore package-lock in case someone npm installed
package-lock.json

# build directory
dist/

# created at build time with tasks/generateSettings.js
src/core/settings/settings.js

# created at build time with tasks/generateFeatureIndex.js
src/extension/features/index.js

# created at build time with tasks/generateFeedChanges.js
src/extension/legacy/features/act-on-change/feedChanges.js
